<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.MetroFireProject" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/red</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/yellow</item>
        <item name="colorOnSecondary">@color/red</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/red</item>
        <!-- NAVIGATION BAR COLOR -->
        <item name="android:navigationBarColor">@color/red</item>

    </style>
    <!-- Splash Screen Style Here. -->
    <style name="splashScreen" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <item name="android:windowBackground" parent="Theme.AppCompat.Light" >@drawable/background_splashscreen</item>
        <!-- Bar containing the time and icons for wifi, battery, etc. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/red</item>
        <!-- Bar at the Bottom of the screen. -->
        <item name="android:navigationBarColor">@color/red</item>
    </style>
</resources>